{
  "name": "rule-repo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:run": "jest",
    "build": "tsc",
    "clean": "rm -rf dist",
    "task:custom": "node scripts/custom-task.js",
    "task:custom:validate": "node scripts/custom-task.js | tee custom-task-output.run.json && node -e \"const fs=require('fs');const t=fs.readFileSync('custom-task-output.run.json','utf8').trim().split(/\\r?\\n/).pop();const o=JSON.parse(t);function a(c,m){if(!c){throw new Error(m)}}a(o.task==='custom-scheduled-task','task mismatch');a(typeof o.package==='string'&&o.package.length>0,'bad package');a(/^\\d{4}-\\d{2}-\\d{2}T/.test(o.date),'bad date');a(Number.isInteger(o.checksum)&&o.checksum>=0&&o.checksum<100000,'bad checksum');a(/^v\\d+\\./.test(o.node),'bad node');if(process.env.FAKE_NOW){a(o.date===new Date(process.env.FAKE_NOW).toISOString(),'date mismatch')}console.log('Custom task output valid')\"",
    "task:custom:validate:fake": "FAKE_NOW=2025-01-02T03:04:05.000Z npm run -s task:custom:validate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.2.1",
    "jest": "^30.0.5",
    "jest-junit": "^16.0.0",
    "ts-jest": "^29.4.1",
    "typescript": "^5.9.2"
  }
}
